#include <stdio.h>
/**
 * main - single digit nums of base 10
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	int num;

	for (num = 32; num < 42; num++)
	{
		putchar(num);
	}
	putchar('\n');
	return (0);
}
