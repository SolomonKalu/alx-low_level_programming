#include "main.h"
#include <stdio.h>

/**
 * main - it all starts here
 * @argc: arguments numbers
 * @argv: array of pointers
 *
 * Return: Always 0.
 */
int main(int argc, char *argv[])
{
	(void)argv;
	printf("%d\n", argc - 1);
	return (0);
}
